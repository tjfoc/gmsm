sudo: false
language: 
  - go
go:
  - 1.8.3

#before_install:
  #- git rm -r --cached .
  #- git add .
  #- git commit -m 'update .gitignore'
install:
  - git checkout develop
  - go get -v github.com/golang/lint/golint
  #- go get -d -t -v github.com/LuckyZhiQiang/gmsm/tree/develop/sm2
  #- go get -d -t -v ./sm3export GOROOT=/home/robin/go
  #- go get -d -t -v ./sm4
  - go build -v ./sm2
  - go build -v ./sm3
  - go build -v ./sm4

script:
  - go vet ./sm2
  - go vet ./sm3
  - go vet ./sm4
  - $HOME/gopath/bin/golint . 
  - export GOPATH="${TRAVIS_BUILD_DIR}/Godeps/_workspace:$GOPATH"
  - export PATH="${TRAVIS_BUILD_DIR}/Godeps/_workspace/bin:$PATH"
  - go test -v ./sm2
  - go test -v ./sm3
  - go test -v ./sm4

after_failure:

 - |
  echo "Error!"

notifications:
  email: false
